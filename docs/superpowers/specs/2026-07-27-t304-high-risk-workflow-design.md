# T-304 高风险端到端流程

T-304 以最小顺序编排复用既有订单、政策、资格、审批、检查点恢复、模拟申请和最终回复门禁。`start` 仅对确认需要人工审批的高风险资格创建审批与检查点；`decide_and_resume` 从检查点读取可信审批 ID，批准后恢复并最多创建一个申请，调整或拒绝不写申请。批准完成必须经过 Response Gate；重复调用复用同一已确认申请。

本任务不提供 HTTP/API、页面、Agent/LangGraph 或通用工作流平台。
