# T-304 高风险端到端流程

## 基本信息

- 所属计划版本：`v0.5.0`
- 状态：2026-07-27 Reviewer 最终审查 PASS；允许创建 T-304 普通任务提交，当前不发布阶段版本 Tag。

## 实际修改

- 新增最小高风险顺序编排，串联订单、政策、资格、审批、检查点恢复、模拟申请和最终回复门禁。
- 批准路径创建且只创建一条模拟申请；调整与拒绝路径不创建申请；重复恢复复用已有结果。
- Reviewer 修复：决定 payload 不再携带审批人，审批人由独立 `ApprovalActorContext` 注入；新增等待审批后的检查点导出/导入、新编排实例恢复的批准、调整和拒绝公开路径回归。

## 验证记录

- 编排组件测试 10 项、审批/恢复/门禁/编排相关基线 52 项、全仓 Python 264 项通过，保留 1 条既有 Starlette TestClient 弃用警告。
- Ruff format/check、mypy（94 个源文件）和 `git diff --check` 通过。

## Reviewer 结论

- 最终结论：PASS；允许创建 T-304 普通任务提交，当前不发布阶段版本 Tag。
- 日期：2026-07-27。
- 证据：项目所有者提供的 T-304 Reviewer 最终结论；最终报告附件未存于工作区。

## 已知限制与风险

- 仅支持固定合成数据中的单商品行高风险场景；没有 HTTP/API、UI、持久数据库或跨进程并发。
