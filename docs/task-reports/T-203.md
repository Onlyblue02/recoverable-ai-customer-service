# T-203 标准退货端到端流程

## 状态

Reviewer 最终审查 PASS；允许创建 T-203 普通任务提交并进入 T-204。本记录不表示阶段三已发布；未开始 T-301，未创建 Tag 或远程推送。

## 修改内容

- 新增最小顺序编排 `StandardReturnWorkflowService.advance()`，显式组合 T-201 路由、T-202 收集、T-102 授权订单、T-101 政策引用、T-103 资格和 T-104 模拟申请。
- 公开请求只有消息；用户身份和已确认槽位由可信上下文注入。
- 信息缺失时只收集；订单不可访问、政策不足、非唯一商品行、资格不符合/需审批、写入失败均安全停止，不创建审批任务。
- 仅低风险 `eligible` 且有真实 `created`/`existing` 申请记录时返回 `COMPLETED`，并携带实际政策引用。
- 越权订单公开结果不包含订单事实或收集快照中的订单号。

## 验证

| 命令 | 实际结果 |
| --- | --- |
| `.venv\\Scripts\\pytest.exe tests/component/orchestration -q -p no:cacheprovider --basetemp=.pytest-tmp-t203-targeted` | 4 passed |
| `.venv\\Scripts\\pytest.exe -q -p no:cacheprovider --basetemp=.pytest-tmp-t203-full` | 205 passed，保留 1 条 Starlette TestClient 上游弃用警告 |
| Ruff format/check、`mypy src tests` | 64 个 Python 文件格式检查通过；Ruff、mypy 通过 |
| 前端 format/lint/test/build | 全部通过；Vitest 1 项通过 |

离线 `uv lock --check` 缺少 `fastapi` 缓存、Docker CLI 不可用的既有阶段限制本轮未记为通过，仍须在 v1.0.0 前关闭或明确记录。

## 模型能力边界

T-203 未接入真实 LLM、模型 SDK、网络模型调用或模型适配器，也不测量或声称任何真实模型能力。本任务完成的是 T-101～T-104、T-201～T-202 的确定性服务组合及其标准退货流程验证；这里的“确定性流程替身”不等同于已实现 Fake 模型适配器。在 T-203 实施时，真实模型供应商、确定性 Fake 适配器合同和专项评测仍属于尚未开始的 T-204，且不得改变订单权限、资格、审批或业务写入的确定性边界。

## Reviewer 结论

- 最终结论：PASS。
- 许可：允许创建 T-203 普通任务提交并进入 T-204。
- 证据：项目所有者提供的阶段三出口 Reviewer 结论；最终报告附件未存于工作区。

## 未覆盖风险

- 只支持单商品行的低风险标准退货；多商品选择、高风险审批、恢复和通用工作流属于 T-301～T-304。
- 不提供持久化会话、HTTP API、Agent、LangGraph 或界面。
