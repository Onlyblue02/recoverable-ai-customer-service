# T-302 中断恢复与重复执行保护

## 基本信息

- 所属计划版本：`v0.5.0`
- TASKS.md 状态：2026-07-27 Reviewer 最终审查 PASS。
- 发布复核状态：允许创建 T-302 普通任务提交并进入 T-303；未创建 Tag 或远程推送。

## 实际修改

- 新增可信、可序列化的审批恢复检查点与公开 `ApprovalRecoveryService`；公开请求只接受工作流和审批 ID。
- 每次恢复均从审批仓储读取并核对用户、订单、商品行、政策、资格、版本和决定，调用方无法伪造审批终态或申请成功。
- 可信批准从保存步骤调用受控模拟申请端口；稳定键使重复恢复复用同一申请。写后未确认状态安全停止，且不盲目重试。
- Reviewer 第二轮修复：检查点保存服务端 workflow/schema 版本和 CAS revision；版本未知、不匹配或陈旧更新一律在写申请前安全失败。

## 验证记录

- Reviewer 第二轮专项 50 passed；阶段三回归 36 passed；全仓 Python 240 passed，保留 1 条既有 Starlette TestClient 弃用警告。覆盖旧/未知工作流版本、schema 不匹配、CAS 冲突及“先持久化后超时”路径。
- Ruff format/check、mypy（86 文件）、前端 Prettier/ESLint/Vitest（1 项）/生产构建和 `git diff --check` 通过。离线 `uv lock --check --offline --cache-dir .uv-cache-t302` 因缓存缺少 FastAPI 未通过；当前环境未发现 Docker CLI，Compose 未执行。

## Reviewer 结论

- 初始及修复后最终结论：PASS；允许创建 T-302 普通任务提交并进入 T-303，当前不发布阶段版本 Tag。
- 日期：2026-07-27。
- 证据：项目所有者提供的 T-302 Reviewer 最终结论；最终报告附件未存于工作区。

## 已知限制与风险

- 当前仓储是可导出/导入的进程内实现，未接入数据库或 HTTP/API。
- 检查点仓储仍为进程内可导出/导入实现，不提供数据库、跨进程并发协调或 HTTP/API。
- T-302 只返回结构化恢复状态；跨领域最终回复质量门禁仍不属于本任务（T-303）。
