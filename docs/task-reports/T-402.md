# T-402 人工审批界面

- 状态：2026-07-28 Reviewer 最终审查 PASS；允许创建 T-402 普通任务提交并进入 T-403，当前不发布 `v1.0.0`。
- 修改：新增审批列表、详情与批准/调整/拒绝表单；服务端以 T-304 可信高风险任务作为列表来源，并由独立服务端审批人上下文调用 T-301。
- 安全：浏览器不能提供审批人、订单、资格、政策或风险事实；终态任务不可重复提交，版本冲突安全返回。
- Reviewer 修复：消费者高风险会话创建并绑定同一 T-304 工作流、检查点与审批任务；批准经既有恢复路径创建唯一模拟申请，调整/拒绝保留无申请终态，消费者刷新读取受控终态。
- 第二轮修复：稳定默认 `HttpApprovalClient`，避免渲染触发重复拉取；详情只展示授权订单、政策 ID/版本/标题/来源、资格状态/结论/规则版本、命中规则、风险原因和终态白名单；同步提交锁阻止快速双击，409 或网络失败后重新读取可信服务端状态。
- 回归覆盖：消费者高风险流程的 approve/adjust/reject、陈旧版本、重复决定和消费者刷新；前端默认 HTTP 加载、重渲染/重新挂载、失败脱敏、快速双击、冲突刷新和证据展示。
- 验证：`.venv\Scripts\pytest.exe tests/component/interfaces/test_approval_console.py tests/component/orchestration/test_high_risk_workflow.py -q -p no:cacheprovider --basetemp=.pytest-tmp-t402-target` 为 12 passed；全仓 pytest 为 275 passed（保留 1 条既有 Starlette TestClient 弃用警告）；前端 Vitest 为 10 passed；Ruff format/check、mypy（98 个源文件）、Prettier、ESLint、生产构建和 `git diff --check` 通过。
- 风险：审批数据为进程内合成演示，重启后丢失；本任务不实现 SSE、生产认证、真实退款或 T-403。

## Reviewer 结论

- 最终结论：PASS；允许创建 T-402 普通任务提交并进入 T-403，当前不发布 `v1.0.0`。
- 日期：2026-07-28。
- 证据：项目所有者提供的 T-402 Reviewer 最终结论；最终报告附件未存于工作区。
